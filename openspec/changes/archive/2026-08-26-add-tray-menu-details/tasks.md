## 1. 显示数据

- [x] 1.1 扩展运行时显示状态，提供已过天数、剩余天数和按当前 `LifeMetric` 计算的百分比；通过 remaining/elapsed 两种状态测试验证天数与百分比来源
- [x] 1.2 增加无 profile 与配置读取失败时的安全显示路径，确保菜单不展示伪造个人数据；通过未配置启动场景验证占位文本和设置/退出操作仍可用
- [x] 1.3 处理已有 profile 但进度或偏好读取失败的情况，使摘要显示为不可用且托盘进程继续存活；通过故障注入启动测试验证“设置”和“退出”仍可用并且不展示旧的或伪造的个人数据

## 2. 原生菜单

- [x] 2.1 创建三个禁用的摘要菜单项并以 `MenuBuilder` 置于设置/退出之前，用分隔线区分信息和操作；通过菜单结构测试验证顺序、禁用状态和操作项保留
- [x] 2.2 将摘要菜单项引用存入 Tauri managed state，并实现统一刷新函数更新已过、剩余和百分比文案；通过状态更新测试验证同一 profile 刷新后的文本
- [x] 2.3 在应用启动和保存设置成功后调用统一刷新入口，使菜单、托盘标题和图标使用同一份已校验状态；通过启动及保存设置冒烟场景验证即时更新

## 3. 刷新与点击行为

- [x] 3.1 增加轻量本地日期变化检查，在跨越日历日期时重新读取配置并刷新托盘摘要；通过模拟日期边界或隔离时钟测试验证跨午夜更新且无重复写入
- [x] 3.4 统一寿命计算与刷新调度使用本地日历日期，修正 UTC 派生日期造成的跨午夜错位；通过固定本地日期边界测试验证非 UTC 时区只刷新一次
- [x] 3.2 移除左键事件中直接打开设置的逻辑，并显式启用原生左键菜单行为；通过 macOS 实机点击验证只显示菜单，选择“设置”后才打开设置窗口
- [x] 3.3 保持摘要菜单项不可点击且不改变 profile 或退出应用；通过逐项点击菜单冒烟验证无副作用

## 4. 验证

- [x] 4.1 在 macOS 实机验证已过天数、剩余天数和百分比的菜单排版、可读性、更新时机及左键菜单行为
- [x] 4.2 运行 `cargo fmt --manifest-path src-tauri/Cargo.toml --check`、`cargo test --manifest-path src-tauri/Cargo.toml --workspace` 和 `cargo check --manifest-path src-tauri/Cargo.toml`，确认工作区通过
