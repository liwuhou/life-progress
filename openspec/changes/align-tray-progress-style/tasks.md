## 1. 配置与兼容

- [x] 1.1 扩展桌面显示偏好模型，加入颜色模式、阈值边界和圆角配置，并为旧 `desktop.toml` 提供默认值；通过配置加载与序列化测试验证兼容性
- [x] 1.2 将图标宽度定义为逻辑点，统一 `22..=200` clamp、默认值和设置滑块范围，并在渲染入口转换为 Retina 物理像素；通过输出尺寸测试验证边界宽度
- [x] 1.3 实现阈值边界归一化，保证 lower/upper 在百分比范围内且 lower 小于 upper；通过无效配置测试验证稳定回退
- [x] 1.4 新增并迁移 `RainbowThreshold` 图形样式的配置枚举与 serde 序列化值，同时兼容现有 `ThresholdBar` 配置；通过配置往返测试验证新旧值可读写

## 2. 图标渲染

- [x] 2.1 重构图形图标渲染为透明画布，并实现内缩轨道、统一圆角裁剪和 Retina 高度布局；通过 RGBA 尺寸、透明边距和圆角边界测试验证几何结果
- [x] 2.2 实现单色与阈值颜色映射，并让普通进度条、圆环和柱状图使用统一颜色语义；通过红/橙/绿边界测试验证 0%、30%、70%、100% 行为
- [x] 2.3 实现 `RainbowThreshold` 彩虹阈值条渲染分支，绘制红/橙/绿分段底色与未填充透明度效果；通过像素颜色分段测试验证默认及自定义阈值
- [x] 2.4 为 remaining 与 elapsed 两种 `LifeMetric` 增加相同百分比颜色映射测试，确认颜色按显示百分比映射且不会自动反转

## 3. 设置与托盘集成

- [x] 3.1 更新原生设置页面，增加颜色模式、阈值边界和 `RainbowThreshold` 样式选项，并将参数传入保存命令；通过设置重开场景验证值能恢复且彩虹样式可达
- [x] 3.2 调整托盘初始化和运行时更新，移除生成图形图标的无条件 macOS template 模式并保留真实 RGBA；通过代码路径测试确认初始化与更新使用同一配置
- [x] 3.3 将保存设置后的托盘刷新统一接入新的渲染配置；通过手动保存设置并观察托盘图标验证颜色和宽度即时生效

## 4. 验证

- [x] 4.1 在 macOS 实机验证 0%、1%、30%、50%、70%、99%、100% 及 60/120/200pt 宽度下的托盘显示，确认无拉伸、方角、裁切或颜色丢失
- [x] 4.2 运行 `cargo fmt --manifest-path src-tauri/Cargo.toml --check`、`cargo test --manifest-path src-tauri/Cargo.toml --workspace` 和 `cargo check --manifest-path src-tauri/Cargo.toml`，确认工作区通过
